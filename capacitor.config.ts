import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'supinfo-4HYBD-tp2-tmdb',
  webDir: 'dist'
};

export default config;
